name: Commit and Push
description: Commit and push changes
runs:
  using: "composite"
  steps:
    - name: Commit and push changes
      run: |
        git add README.md
        git commit -m "Update README with metrics"
        git push
      shell: bash
